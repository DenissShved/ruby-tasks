app.rb is main class for this script.

Was created dummy information on Pipedrive in order to develop this script

Credentials are saved as environment variables. Thus, script is more flexible

Creating url for requests through ConnectionManager and when calling get method

This script uses two external libraries: "HTTParty" and "csv"

Output file is generated at the same directory and name depends on method called

Output file is csv file with first row being the names of the columns

Inside "TODO's" cam be found
